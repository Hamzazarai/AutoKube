apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hub-db-pvc
  namespace: jhub
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ""
  volumeName: hub-db-pv